
nvgstcapture-1.0
ls -l /dev/video*

sudo apt install v4l-utils
v4l2-ctl --list-devices

sudo python /opt/nvidia/jetson-io/jetson-io.py  

|                Configure Jetson 24pin CSI Connector                |
|                 Configure for compatible hardware                  |
|                          Camera IMX219-A                           |
|                          Save pin changes                          |
|                Save and reboot to reconfigure pins                 |

git config --global lassi04kokkonen@gmail.com
git config --global user.name classikk
git remote add origin git@github.com:classikk/Jetson.git
ls -al ~/.ssh
ssh-keygen -t ed25519 -C "lassi04kokkonen@gmail.com"
#copy this to git
cat ~/.ssh/id_ed25519.pub 

v4l2-ctl --list-formats-ext

find /usr/include -name opencv.hpp


CUDA
https://forums.developer.nvidia.com/t/cuda-nvcc-not-found/118068

If you used the Nano SD card image, then yes, it already has the CUDA Toolkit installed. Check under /usr/local/cuda to verify that it’s there.

Check that your ~/.bashrc file has these lines at the end, and if not, add them and restart your terminal:

$ export PATH=/usr/local/cuda/bin${PATH:+:${PATH}}
$ export LD_LIBRARY_PATH=/usr/local/cuda/lib64\
                         ${LD_LIBRARY_PATH:+:${LD_LIBRARY_PATH}}

